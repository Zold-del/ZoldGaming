# Exemple de fichier .env (NE PAS mettre de valeurs réelles ici)
# Copiez ce fichier en `.env` localement et remplissez les valeurs.

# Environnement
NODE_ENV=production

# URL du client
CLIENT_URL=https://your-domain.example

# MongoDB Atlas URI (ne pas committer de vraies credentials)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/<database>?retryWrites=true&w=majority

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRE=7d

# Port local (utilisé seulement en local)
PORT=3000

# Cookies
COOKIE_EXPIRE=7
